<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pfe.polytech.Vuzix_M100_entrepot" >

    <!-- Permission pour le micro
    <uses-permission android:name="android.permission.RECORD_AUDIO" />-->
    <!-- Permission pour la caméra-->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Permission pour internet-->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <!--android:name=".MainActivity"-->
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
             >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- Ajout d'un second activity pour le scan -->
        <activity
            android:name=".ScanActivity"
            android:theme="@style/AppOverlayTheme"
            android:label="@string/scan_cb">
        </activity>
    </application>



</manifest>
<!--  android:naMainActivityvity"-->